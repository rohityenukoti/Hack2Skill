{
  "name": "chikitsalay-setu-functions",
  "description": "Cloud Functions for Chikitsalay Setu — Gemini AI, Speech-to-Text, BigQuery, Translation",
  "main": "index.js",
  "type": "module",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "lint": "node --check index.js && node --check ai.js && node --check speech.js && node --check bigquery.js && node --check translation.js && node --check seed.js",
    "serve": "firebase emulators:start --only functions",
    "shell": "firebase functions:shell",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "@google-cloud/bigquery": "^7.9.0",
    "@google-cloud/speech": "^6.7.0",
    "@google-cloud/translate": "^8.5.0",
    "@google/generative-ai": "^0.21.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.1.0"
  },
  "private": true
}
